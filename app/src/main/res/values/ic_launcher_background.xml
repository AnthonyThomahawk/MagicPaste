<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Sampled from the edges of the icon artwork, so the adaptive background is
      indistinguishable from the foreground's own backdrop where the two meet.
    -->
    <color name="ic_launcher_background">#171B33</color>
</resources>
